import React from 'react';

function OptionTitle({ no, title, note }) { return <div className="option-title"><span>{no}</span><h2>{title}</h2><small>{note}</small></div>; }
export default OptionTitle;

