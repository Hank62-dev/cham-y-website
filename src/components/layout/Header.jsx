import { useState } from 'react';
import { ArrowUpRight, Menu, X } from 'lucide-react';
import { asset } from '../../lib/assets';

function Header({ page, go }) { const [open, setOpen] = useState(false); const navigate = (next) => { setOpen(false); go(next); }; return <><header className="site-header"><a className="brand" onClick={() => navigate('home')}><img src={asset('logoChamY.jpg')} /><span>CHẠM Ý<small>chạm vào cá tính</small></span></a><button className={`menu-toggle ${open ? 'is-open' : ''}`} onClick={() => setOpen((value) => !value)} aria-label={open ? 'Đóng menu' : 'Mở menu'} aria-expanded={open}>{open ? <X size={20} /> : <Menu size={20} />}</button><nav className={open ? 'mobile-open' : ''}>{[['home', 'Trang chủ'], ['shop', 'Cửa hàng'], ['guide', 'Hướng dẫn'], ['about', 'Về Chạm Ý']].map(([key, label]) => <button className={page === key ? 'active' : ''} onClick={() => navigate(key)} key={key}>{label}</button>)}<button className="mobile-customize" onClick={() => navigate('customize')}>Tự phối ngay <ArrowUpRight size={17} /></button></nav><button className="nav-cta" onClick={() => navigate('customize')}>Tự phối ngay <span>↗</span></button></header>{open && <button className="mobile-menu-backdrop" onClick={() => setOpen(false)} aria-label="Đóng menu"></button>}</>; }
export default Header;
